-- Create user with email and password (admin/CLI only)
--
-- 1. Generate bcrypt hash for your password using htpasswd:
--    htpasswd -nbBC 10 "" "YOUR_PASSWORD" | cut -d: -f2
--
-- 2. Generate UUIDs (uuidgen or cat /proc/sys/kernel/random/uuid)
--
-- 3. Replace placeholders below and run against your database.

-- Example (replace placeholders):
-- INSERT INTO users (id, first_name, last_name, email, password, sso_id, stage, status, created_at, updated_at)
-- VALUES (
--   'xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx',  -- id (UUID)
--   'Admin',
--   'User',
--   'admin@example.com',
--   '$2y$10$...',  -- bcrypt hash from step 1
--   'xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx',  -- sso_id (UUID)
--   'Completed',
--   'Active',
--   NOW(),
--   NOW()
-- );
